.env
data/.cookies.json
data/.session_cookies.json
__pycache__/
*.pyc
.venv/
venv/
# Keep data/tweets/*.json in git — that is intentional (local tweet storage).
# Exclude only the session cookie files above.
