.env
data/.cookies.json
data/.session_cookies.json
__pycache__/
*.pyc
.venv/
venv/

# Keep tweet JSON snapshots tracked; only ignore hidden cookie files (above).
# data/tweets/ is intentionally tracked.
